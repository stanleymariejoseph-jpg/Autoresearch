[project]
name = "autoresearch-lab"
version = "0.2.0"
description = "Clean-room autonomous experiment loop with optional Mistral code agent"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
autoresearch-lab = "autoresearch_lab.cli:main"


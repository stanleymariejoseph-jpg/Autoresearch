[project]
name = "autoresearch-lab"
version = "0.1.0"
description = "Clean-room autonomous experiment loop"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
autoresearch-lab = "autoresearch_lab.cli:main"

